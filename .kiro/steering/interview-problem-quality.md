# Interview Problem Quality Steering

Use `context-manifest.yaml` as the source of required context.
For problem authoring and review, follow `SKILL.md` and the references under
`references/`. Run validators in `scripts/` before promotion.

Do not place generation/review tooling in the runtime `interview-mcp` repo.
